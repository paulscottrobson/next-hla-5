HLA-Pythonish syntax.
One instruction per line.
identifiers can be [a-z_][a-z90-9_.:]

global x,y,z

def name(p1,p2,p3):	
	<ident> = <expr>					# declares ident if not known
	<ident>!<term> = <expr>				# <expr> are <term> <op> <term> <op> <term> - identifier or constant.
	if <expr>:
		<xxx>
		<xxx>
		<xxx>
	while <expr>:
		<xxx>
		<xxx>
	for <expr>:
		<xxx>
		<xxx>
		<xxx>

	<ident>(<expr>,<expr>,<expr>)


